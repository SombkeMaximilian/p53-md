# Force Fields

Force field(s) typically not bundled with GROMACS were used in this project.
They can be downloaded using `download.sh` in the same directory, or by
manually downloading them and placing them in this folder.

List:

- a99SB-disp
